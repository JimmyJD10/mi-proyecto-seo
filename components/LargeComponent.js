export default function LargeComponent() {
  return (
    <div>
      <h2>Componente grande cargado dinámicamente</h2>
      <p>Este componente se carga con lazy loading usando next/dynamic.</p>
    </div>
  );
}
